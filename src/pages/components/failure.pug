div(class="modal failure response__modal")
  h2(class="title failure__title") 
    | Что-то пошло&nbsp;
    br(class="break break--tablet")
    | не так!
  p(class="failure__message") Проверьте поля, выделенные красным, скорее всего вы забыли их заполнить
  = '\n'
  button(class="btn failure__btn" type="button") Ок
